import PublicRecipientResponseClient from '@/components/messaging/PublicRecipientResponseClient';

export default function PublicMessagingConfirmPage() {
    return <PublicRecipientResponseClient />;
}
